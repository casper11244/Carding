intalar python

intalar java

intalar sqlite3


-- Crear base de datos (opcional, si no existe)
sqlite3 tarjetas.db

-- si ya existe .open tarjetas.db

-- Crear tabla
CREATE TABLE tarjetas (
    id_tarjeta INTEGER PRIMARY KEY AUTOINCREMENT,
    numero TEXT NOT NULL,
    mes INTEGER NOT NULL,
    anio INTEGER NOT NULL,
    ccv INTEGER NOT NULL
);


-----------------------------------------------------------------------
intalar rust

ejecutar

intalar sudo dnf install gcc

sudo dnf install pkg-config openssl-devel

-------------------------------------------------------------------------
1. Instalar Tor
bash
sudo apt update
sudo apt install tor -y
2. Iniciar Tor
bash
sudo systemctl start tor
sudo systemctl enable tor  # Para que inicie automáticamente
3. Verificar que Tor está corriendo
bash
sudo systemctl status tor
# Debe decir "active (running)"

# Verificar puerto
ss -tlnp | grep 9050
# Debe mostrar: 127.0.0.1:9050